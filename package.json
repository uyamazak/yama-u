{
  "devDependencies": {
    "@nuxt/content": "^3.11.2",
    "@nuxthq/studio": "^2.2.1",
    "@oxc-parser/binding-linux-x64-gnu": "^0.112.0",
    "better-sqlite3": "^12.6.2",
    "nuxt": "^4.3.1",
    "pagefind": "^1.3.0",
    "vitepress": "^1.6.4",
    "vitepress-plugin-pagefind": "^0.4.14"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "deploy": "vitepress build && firebase deploy",
    "d": "vitepress build && firebase deploy",
    "dev": "vitepress dev",
    "studio:dev": "nuxt dev",
    "studio:build": "nuxt build",
    "studio:preview": "nuxt preview",
    "g": "node scripts/generate-post.mjs",
    "u": "git add -A && git commit -m 'update' && git push"
  },
  "dependencies": {
    "@oxc-transform/binding-linux-x64-gnu": "^0.112.0"
  }
}
